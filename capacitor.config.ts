import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ernestoverdugo.guia2',
  appName: 'iguia2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
